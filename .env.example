DATA_DIR=./data
# Set to 1 only on a trusted private deployment to open Cove as the first active administrator without a login screen.
COVE_DISABLE_AUTH=0
# Optional: run MCP with this account's workspace permissions.
COVE_MCP_USER_EMAIL=

# Optional server-only integration credentials. Never expose these with NEXT_PUBLIC_ names.
GITHUB_TOKEN=
